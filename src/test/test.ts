import { log } from "console";
import checkDifference from "../utils/check-difference";
import newPersianDate from "../utils/new-persian-date";
import DateObject from "date-object";

export default function test() {
  //   log("test 1");
  //   const firstDate = newPersianDate();
  //   const secondDate = new DateObject(firstDate).subtract(30, "minutes");
  //   log("check diffrence: ", checkDifference(secondDate, firstDate, "hour"));
}
